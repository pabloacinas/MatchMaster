<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_matchmaster"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_matchmaster_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MatchMaster">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".CrearLigaActivity"
            android:exported="false" />
            
        <activity
            android:name=".AgregarEquiposActivity"
            android:exported="false" />
            
        <activity
            android:name=".LigaDetalleActivity"
            android:exported="false" />
            
        <activity
            android:name=".EditarPartidoActivity"
            android:exported="false" />
        
    </application>

</manifest>